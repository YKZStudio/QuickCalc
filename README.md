# QuickCalc

一个轻量（可能？）、顺手、好用的计算器

QuickCalc 是一款以键盘为中心的快速桌面计算器。默认按 `Ctrl + Shift + Space` 呼出，再按一次隐藏；窗口始终置顶、无边框，并在失去焦点时自动隐藏。

项目当前处于 **v0.1 框架阶段**：桌面生命周期、持久化、表达式求值核心、基础界面和 Windows 安装包自动发布流程已经搭好；代码签名与更完整的设置界面列在路线图中。

## 设计目标

- Windows 优先，随后验证 macOS 与 Linux。
- 后台常驻时尽量少占用 CPU 和内存。
- 不依赖网络，表达式与历史记录只保存在本机。
- 每次成功计算先同步写盘，再把结果返回界面；历史记录最多保留 50 条。
- 交互足够快：一次回车计算，同一表达式再次回车复制结果。

## 已搭建的能力

- 全局快捷键显示/隐藏主窗口。
- 开机自启动；自启动时静默驻留后台。
- 无边框、置顶、跳过任务栏、失焦隐藏。
- 四则运算、取模、乘方、根式与常用数学函数。
- 64 位有符号整数位运算：`&`、`|`、`xor`、`~`、`<<`、`>>`。
- 二/八/十/十六进制字面量和输出转换。
- 用户变量赋值；内置只读变量 `pi`、`e`、`res`。
- 圆括号、方括号、花括号自动补全并统一参与求值。
- 自动把全角数字、中文括号和常见中文数学标点转换成解析器支持的英文形式。
- 设置、变量、`res` 和 50 条历史记录的崩溃友好型原子持久化。

## 表达式示例

| 目的 | 输入 | 结果示例 |
| --- | --- | --- |
| 四则运算 | `(2 + 3) * 4` | `20` |
| 乘方 | `2 ^ 10` 或 `2 ** 10` | `1024` |
| 根式 | `sqrt(81)`、`root(32, 5)` 或 `√81` | `9`、`2` |
| 位运算 | `(0xff & 0b1010) << 2` | `40` |
| 异或 | `5 xor 3` | `6` |
| 进制转换 | `255 -> hex` | `0xff` |
| 定义变量 | `tax = 0.09` | `0.09` |
| 使用变量 | `199 * (1 + tax)` | `216.91` |
| 使用上次结果 | `res / 2` | 上次结果的一半 |

进制输出目标支持 `bin`、`oct`、`dec`、`hex`。位运算只接受可表示为 64 位有符号整数的值。`^` 专用于乘方，异或使用 `xor`，避免计算器语义歧义。

## 开发

前置环境：Node.js 20+、Rust 1.77.2+，以及目标平台所需的 [Tauri 2 系统依赖](https://v2.tauri.app/start/prerequisites/)。

```bash
npm install
npm run tauri dev
```

常用检查：

```bash
npm test
npm run typecheck
npm run build
cd src-tauri && cargo test
```

## Windows 发布

推送与应用版本一致的 `v<semver>` 标签后，GitHub Actions 会自动构建 Windows x64 NSIS `.exe` 与 MSI，并发布到 GitHub Releases。具体版本同步、标签命令和未签名安装包说明见 [发布说明](docs/RELEASE.md)。

## 技术选型

- **Tauri 2 + Rust**：复用系统 WebView，后台逻辑由原生进程承担，适合小体积、低常驻开销的桌面工具。
- **原生 TypeScript + CSS**：不引入前端框架和运行时状态库，降低启动与维护成本。
- **自研小型 Pratt 风格解析器**：明确控制运算符优先级、只读变量和位运算语义，不执行任意代码。
- **JSON 原子写入**：文件可读、便于诊断；临时文件与备份文件用于处理中途终止。

更完整的行为定义见 [产品规格](docs/PRODUCT_SPEC.md)，实现边界见 [架构说明](docs/ARCHITECTURE.md)，发布流程见 [发布说明](docs/RELEASE.md)，后续工作见 [路线图](docs/ROADMAP.md)。

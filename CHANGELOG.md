# Changelog

本项目的重要变更记录于此，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.2.3] - 2026-08-04

### 新增

- 首次使用引导，说明全局快捷键、计算方式和常用退出命令。
- 历史记录栏的收起与展开按钮。
- `/exit`、`/quit` 隐藏窗口，以及 `/shutdown` 彻底退出。
- 自动补全激活时可按 Enter 或 Tab 接受当前项。

### 修复

- 删除变量或缩短结果内容后，窗口会根据真实内容高度平滑回缩。
- 引导完成状态兼容旧版原生后端，不再显示缺少 `complete_onboarding` 命令的错误。
- 自动更新检查统一读取应用版本，避免版本号不同步。

### 优化

- 空历史提示在可用区域垂直居中，状态提示字体更紧凑。
- 降低重复窗口尺寸计算与跨进程调用。
